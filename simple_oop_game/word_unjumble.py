#!/usr/bin/env python
from os.path import expanduser
from os import listdir, join, isfile, isdir
from os import walk

"""
Word Unjumbler:
    Unjumbles a word, returning a list of all the possible words via the
    English Dictionary
"""

dir_path = expanduser("~")
files = []

for path in listdir(dir_path):
    if 

def word_unjumbler(jumble: str) -> list:
    pass

